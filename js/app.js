console.log('Aloha!');

projLink = url => {
  window.location.pathname = url;
};

let projects = document.getElementsByClassName('proj-space');

// projects.addEventListener('click', projLink('./projects/grok-mobile.html'), false);
